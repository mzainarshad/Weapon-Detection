# Architecture

> Auto-generated by /map on 2026-04-12

## Overview

This project is an automated, intelligent Video Surveillance Enhancement system. It transitions traditional CCTV from passive recording to proactive alerting by using deep learning (YOLOv8) to detect behavioral anomalies such as weapons.

```
┌─────────────────────────────────────────┐
│        Webcam / IP Camera (RTSP)        │
├─────────────────────────────────────────┤
│            [Input Layer]                │
│       OpenCV VideoCapture / Streaming   │
├─────────────────────────────────────────┤
│         [Processing Layer]              │
│       YOLOv8 Inference Engine           │
├─────────────────────────────────────────┤
│            [Backend API]                │
│       Flask Logic + Auth + Logs         │
├─────────────────────────────────────────┤
│           [Frontend UI]                 │
│    React Dashboard + Real-time Alerting │
└─────────────────────────────────────────┘
```

## Components

### Frontend (Web UI)
- **Purpose:** Visualization and history management.
- **Location:** `Weapon-Detection/frontend/`
- **Tech:** React.js, Vite, Axios.
- **Features:** Live camera grids (planned), real-time alert popups (planned), incident history logs.

### Backend (API Layer)
- **Purpose:** Serving detection data to the frontend.
- **Location:** `Weapon-Detection/backend/`
- **Tech:** Flask, Flask-CORS, Pandas.
- **Features:** Serving incident logs from CSV.

### AI Engine (Detection Script)
- **Purpose:** Real-time weapon detection using YOLOv8.
- **Location:** `Weapon-Detection/detect_realtime.py`
- **Tech:** YOLOv8 (Ultralytics), OpenCV.
- **Logic:** Frame capture -> YOLOv8 Inference -> Image saving -> CSV Logging (Pending).

### Data Layer
- **Purpose:** Persistence of detections.
- **Location:** `Weapon-Detection/detections/`
- **Files:** Image snapshots (`*.jpg`) and log file (`detection_log.csv`).

## Data Flow

1. **Detection:** `detect_realtime.py` captures frames from webcam/RTSP, runs YOLOv8 model.
2. **Snapshot:** If a weapon is detected, an image is saved to `detections/`.
3. **Logging:** (TBD) Detection details (timestamp, class, confidence) should be written to `detections/detection_log.csv`.
4. **Backend Fetching:** `backend/app.py` reads `detection_log.csv` and serves it via `/api/logs`.
5. **Frontend Display:** `frontend/src/App.jsx` (TBD) fetches data from `/api/logs` and displays it in a dashboard.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Roboflow | Dataset | Source for training data |
| Flask API | REST | Communication between React and Python |
| YOLOv8 | Model | Core detection logic |

## Technical Debt

- [ ] **Logging Implementation:** `detect_realtime.py` saves images but does not yet write incident records to `detection_log.csv`.
- [ ] **Backend Path Bug:** `backend/app.py` has a redundant nested `Weapon-Detection` folder in its `DETECTIONS_DIR` path calculation.
- [ ] **Frontend Integration:** The React frontend is currently a boilerplate and needs to be replaced with a real dashboard that calls the backend API.
- [ ] **Missing Weights:** The system requires `weights/best.pt` which is not currently present in the repository.
- [ ] **Absolute Paths in Config:** `data.yaml` may contain absolute paths that break portability.

## Conventions

**Naming:** Action-oriented script naming (`detect_realtime.py`, `train.py`).
**Structure:** Multi-layered architecture with separate folders for `frontend`, `backend`, and `ML` scripts.
